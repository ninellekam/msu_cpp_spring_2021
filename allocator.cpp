#include "test.hpp"

int main() {
	test();
	// std::ios_base::sync_with_stdio(false);
	// std::cin.tie(NULL);

	// std::string s;
	// size_t size;
	// std::cin >> s;
	// Allocator alloc;
	// while (std::cin >> s) {
	// 	if (s == "makeAllocator") {
	// 		std::cin >> size;
	// 		alloc.makeAllocator(size);
	// 	}
	// 	if (s == "alloc") {
	// 		size = 0;
	// 		std::cin >> size;
	// 		alloc.alloc(size);
	// 	}
	// 	if (s == "reset") {
	// 		alloc.reset();
	// 	}
	// }
	// std::cout << "Exit!";
	return (0);
}